[pytest]
# Parallel per Default (pytest-xdist): die Suite ist stark solver-/ML-lastig
# und CPU-gebunden. -n auto nutzt alle Kerne; --dist loadscope hält die Tests
# EINES Moduls im selben Worker (in Reihenfolge) - robust gegenüber modul-
# lokalem Prozess-State (z.B. optimizer._warm_cache, forecast._ML_CACHE).
# Braucht pytest-xdist (requirements-dev.txt).
addopts = -n auto --dist loadscope

# LAUFZEIT: der volle Lauf braucht rund 3 min (745 Tests, gemessen 03.09.2026
# auf diesem Rechner). Entscheidend dafuer sind die Thread-Variablen in
# conftest.py - OHNE sie waren es 12 min, weil jeder der vier xdist-Worker
# zusaetzlich eigene OpenMP-Threads startete (bis zu 16 auf vier Kernen). Wer
# die Suite anders aufruft und langsam ist, prueft zuerst das.
#
#   pytest                    alle Tests, ~2:55
#   pytest -m "not slow"      ~2:21 - die Markierung deckt nur 19 von 745
#                             Tests ab und bringt seit der Thread-Korrektur
#                             kaum noch etwas. Im Zweifel den vollen Lauf.
#   pytest tests/test_x.py    ein Modul, meist wenige Sekunden
markers =
    slow: rechenintensiver MILP-/ML-Test (via -m "not slow" abwaehlbar)

# PuLP 3.x warnt bei jeder direkten LpVariable-Konstruktion vor der PuLP-4.0-API
# ("Constructing LpVariable ... directly is deprecated"). Der Optimierer erzeugt
# pro Solve hunderte Variablen; über tausende Solver-Läufe in den Tests sind das
# zehntausende identische, harmlose Zeilen -> gezielt ausblenden (Migration auf
# prob.add_variable erst nötig, wenn PuLP 4.0 kommt).
filterwarnings =
    ignore:Constructing LpVariable:DeprecationWarning
