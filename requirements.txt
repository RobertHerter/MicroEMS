# EMS – Python dependencies (Raspberry Pi OS Trixie / Debian 13)
# Install into a venv:  python3 -m venv .venv && . .venv/bin/activate && pip install -r requirements.txt
# Reproduzierbare Installation (exakt getestete Versionen): pip install -r requirements.lock

PyYAML>=6.0.3
pandas>=3.0.5
numpy>=1.24
python-dateutil>=2.8

# Optimierung (MILP). PuLP bringt den CBC-Solver bereits mit.
PuLP>=3.3.2
# HiGHS ist der PRODUKTIV genutzte Solver: schneller als das gebuendelte CBC und
# mit festem Seed deterministisch (gleicher Input -> gleicher Plan). Darauf bauen
# Plan-Stabilitaet, Warmstart-Artefakt-Erkennung und der Kalt-Resolve auf. Fehlt
# es, faellt der Optimierer sichtbar auf CBC zurueck - lauffaehig, aber ohne
# garantierten Determinismus.
highspy>=1.15.1

# Feiertage (Verbrauchsprognose)
holidays>=0.102

# InfluxDB – beide Clients, je nach konfigurierter Version wird einer genutzt
influxdb>=5.3            # InfluxDB 1.x (InfluxQL)
influxdb-client>=1.50.0    # InfluxDB 2.x (Flux)

# MQTT-Ausgabe an Homey
paho-mqtt>=1.6

# Dashboard (selbst-enthaltenes interaktives HTML)
plotly>=6.9.0

# Machine Learning (für Lastprognose)
scikit-learn>=1.9.0

# Optionale E3DC-Anbindung per RSCP
pye3dc>=0.7
# Optional: freie PV-Ertragsprognose (ems/pvforecast.py, Alternative zu Solcast)
pvlib>=0.15.2
